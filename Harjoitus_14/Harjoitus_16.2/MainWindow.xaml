<Window x:Class="DistanceConverter.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Distance Converter" Height="150" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBox Name="txtKilometers" Grid.Row="0" Margin="10" Width="200" />
        <TextBox Name="txtMiles" Grid.Row="1" Margin="10" Width="200" />
        <Button Name="btnKmToMiles" Grid.Row="2" Margin="10" Width="100" Height="30" Content="Convert" Click="btnKmToMiles_Click" />
    </Grid>
</Window>